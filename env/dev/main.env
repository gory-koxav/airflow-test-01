# ============================================
# DEV Environment - Main Server Configuration
# ============================================
# For development: main and workers run on the SAME machine
# Workers connect via Docker internal network

# Port Configuration (Host ports - for external access and conflict avoidance)
# These ports are exposed to the host machine
POSTGRES_PORT=55432
REDIS_PORT=56379
API_SERVER_PORT=8080
FLOWER_PORT=5555
