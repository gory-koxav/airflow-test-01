# =============================================================================
# DEV Environment Override - Worker
# =============================================================================
# Layer: docker-compose_worker.yaml + THIS FILE (+ gpu.yaml if WORKER_GPU=true)
#
# Purpose: Network configuration for development (same machine as main server)
#
# Shared Storage:
#   HOST_DATA_PATH=./data (local project folder, set in worker env files)
# =============================================================================

networks:
  worker-network:
    external: true
    name: airflow-test-01_airflow-network
